{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api|auth|socket.io|health).*)",
      "destination": "/index.html"
    }
  ]
}
