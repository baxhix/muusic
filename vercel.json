{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api|auth|admin|socket.io|health).*)",
      "destination": "/index.html"
    }
  ]
}
